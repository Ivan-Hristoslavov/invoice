import { Metadata } from "next";
import { APP_NAME } from "@/config/constants";

export const metadata: Metadata = {
  title: `Create New Invoice | ${APP_NAME}`,
  description: "Create a new invoice for your clients and track payments",
}; 