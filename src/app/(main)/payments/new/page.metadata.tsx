import { Metadata } from "next";
import { APP_NAME } from "@/config/constants";

export const metadata: Metadata = {
  title: `Запис на плащане | ${APP_NAME}`,
  description: "Запишете ново плащане за вашите фактури",
}; 