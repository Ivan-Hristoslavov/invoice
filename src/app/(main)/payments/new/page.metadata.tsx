import { Metadata } from "next";
import { APP_NAME } from "@/config/constants";

export const metadata: Metadata = {
  title: `Record Payment | ${APP_NAME}`,
  description: "Record a new payment for your invoices",
}; 