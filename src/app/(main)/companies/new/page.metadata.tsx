import { Metadata } from "next";
import { APP_NAME } from "@/config/constants";

export const metadata: Metadata = {
  title: `Add New Company | ${APP_NAME}`,
  description: "Create a new company for your invoices",
}; 