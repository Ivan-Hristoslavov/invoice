import { Metadata } from "next";
import { APP_NAME } from "@/config/constants";

export const metadata: Metadata = {
  title: `Добавяне на нова компания | ${APP_NAME}`,
  description: "Създайте нова компания за вашите фактури",
}; 